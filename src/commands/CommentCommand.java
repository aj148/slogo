package commands;

import model.Model;


public class CommentCommand extends ZeroInputCommand {

    // This is a null class. It is not supposed to do anything.

    @Override
    public double executeCommand (Model model) {
        return 0;
    }
}
